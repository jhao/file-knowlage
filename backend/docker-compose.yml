services:
  web:
    build: .
    ports:
      - "5009:5009"
    environment:
      SECRET_KEY: change-me
      DATABASE_URI: sqlite:////app/instance/ailibrary.sqlite
    volumes:
      - ./instance:/app/instance
